# 桌球對戰戰績（Vercel 版）

- React + Vite + TypeScript + Tailwind
- 精簡 UI 元件在 `src/components/ui/*`
- 本地儲存（localStorage）版，可匯入/匯出 JSON

## 使用
```bash
npm install
npm run dev
```
部署到 Vercel 時請確定根目錄包含 `package.json`，且已安裝 `@vitejs/plugin-react`（這個專案已內建）。
